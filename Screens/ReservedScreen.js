import { View, Text, StyleSheet, Image, TouchableOpacity, TextInput } from 'react-native'


export default function ReservedScreen({ navigation }) {

    return (
        <View>
            <Text>ReservedScreen</Text>
        </View>
    )
}